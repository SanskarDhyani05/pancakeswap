import history from "../history";

export enum Paths {
  root = "/",
  home = "/home",
  trade = "/trade",
  liquidity="/liquidity",
  addLiquidity="/addLiquidity",
  farms="/farms",
  pools="/pools",
  lottery="/lottery",
}
